Idées :
 - Construire l'automate de aho corasick pour les mots de pouvoir
 - Un automate classique fonctionne sur
   A* = Empty | Cons A A*
   (on pourrait ajouter A* ++ A* dans l'arbre)
 - Il serait bien de faire fonctionner l'automate sur
   A' = Empty | Cons A A' | (A' || A')
     où x || y ~ union de deux langages
   Il faut replacer l'état de l'automate par 
   Il y a sans doute des trucs utiles ici : https://www.fpcomplete.com/user/edwardk/moore/for-less

 - Pour avoir les 300pts de la premiere utilisation de chaque mot, on duplique les états

 - Extension pour du matching imbriqué ?
   (C C C match C C deux fois)
  